import React from 'react';
import MuiSlider from '@mui/material/Slider';

export default function Slider(props) {
    return (
        <MuiSlider defaultValue={30} className="text-teal-600" />
    );
}

